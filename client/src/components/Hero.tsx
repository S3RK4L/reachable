export default function Hero() {
  return (
    <section className="text-center pt-12 pb-6 md:pt-16 md:pb-8 px-4">
      <h2 className="text-4xl md:text-5xl font-extrabold text-ink tracking-tight leading-tight">
        Stop scrolling.
        <br />
        <span className="text-ocean">Start going.</span>
      </h2>
      <p className="mt-4 text-ink-muted text-lg md:text-xl max-w-lg mx-auto leading-relaxed">
        Find events you can actually reach, filtered by real journey time, not&nbsp;a&nbsp;pin&nbsp;on&nbsp;a&nbsp;map.
      </p>
    </section>
  );
}
